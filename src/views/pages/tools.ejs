<div class="p-6 bg-white rounded-lg shadow">
    <div class="flex items-center mb-6">
        <div class="bg-green-200 rounded-full p-3 mr-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-green-600" fill="none" viewBox="0 0 24 24"
                stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4" />
            </svg>
        </div>
        <div>
            <h1 class="text-3xl font-bold">الإعدادات العامة للنظام</h1>
            <p class="text-gray-600">إدارة الإعدادات العامة وأدوات النظام</p>
        </div>
    </div>

    <!-- Tabs -->
    <div class="border-b border-gray-200 mb-6">
        <nav class="flex space-x-4 rtl:space-x-reverse" aria-label="Tabs">
            <button id="tab-general"
                class="px-4 py-2 rounded-t-lg border border-b-0 border-gray-300 bg-white text-gray-700 font-semibold focus:outline-none">الإعدادات
                العامة</button>
            <button id="tab-tools"
                class="px-4 py-2 rounded-t-lg border border-b-0 border-gray-300 text-gray-500 hover:text-gray-700 focus:outline-none">أدوات
                النظام</button>
            <button id="tab-maintenance"
                class="px-4 py-2 rounded-t-lg border border-b-0 border-gray-300 text-gray-500 hover:text-gray-700 focus:outline-none">الصيانة</button>
        </nav>
    </div>

    <!-- Tab Contents -->
    <div id="tab-content-general" class="tab-content">
        <div class="bg-gray-50 p-6 rounded-lg border border-gray-200">
            <h2 class="text-xl font-bold mb-4">الإعدادات الأساسية</h2>
            <form class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label class="block mb-1 font-semibold">اسم النظام</label>
                    <input type="text" value="نظام إدارة الجوائز والمعايير"
                        class="w-full border border-gray-300 rounded px-3 py-2" />
                </div>
                <div>
                    <label class="block mb-1 font-semibold">إصدار النظام</label>
                    <input type="text" value="2.1.0" class="w-full border border-gray-300 rounded px-3 py-2" />
                </div>
                <div>
                    <label class="block mb-1 font-semibold">لغة النظام</label>
                    <select class="w-full border border-gray-300 rounded px-3 py-2">
                        <option>العربية</option>
                        <option>English</option>
                    </select>
                </div>
                <div>
                    <label class="block mb-1 font-semibold">المنطقة الزمنية</label>
                    <select class="w-full border border-gray-300 rounded px-3 py-2">
                        <option>الرياض (GMT+3)</option>
                        <option>UTC</option>
                        <option>GMT</option>
                    </select>
                </div>
                <div class="flex items-center space-x-4 rtl:space-x-reverse mt-4">
                    <label class="font-semibold">إشعارات البريد الإلكتروني</label>
                    <input type="checkbox" checked class="toggle-checkbox" />
                </div>
                <div class="flex items-center space-x-4 rtl:space-x-reverse mt-4">
                    <label class="font-semibold">سجلات النظام</label>
                    <input type="checkbox" checked class="toggle-checkbox" />
                </div>
                <div class="flex items-center space-x-4 rtl:space-x-reverse mt-4">
                    <label class="font-semibold">النسخ الاحتياطي التلقائي</label>
                    <input type="checkbox" checked class="toggle-checkbox" />
                </div>
            </form>
        </div>
    </div>

    <div id="tab-content-tools" class="tab-content hidden">
        <div class="bg-gray-50 p-6 rounded-lg border border-gray-200 grid grid-cols-1 md:grid-cols-3 gap-4">
            <button
                class="flex items-center justify-center space-x-2 rtl:space-x-reverse border border-gray-300 rounded px-4 py-3 hover:bg-red-500 hover:text-white transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round"
                        d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <span>مسح ذاكرة التخزين المؤقت</span>
            </button>
            <button
                class="flex items-center justify-center space-x-2 rtl:space-x-reverse border border-gray-300 rounded px-4 py-3 hover:bg-red-500 hover:text-white transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M8 17l4-4m0 0l4-4m-4 4v12" />
                </svg>
                <span>إنشاء نسخة احتياطية</span>
            </button>
            <button
                class="flex items-center justify-center space-x-2 rtl:space-x-reverse bg-red-500 text-white border border-red-500 rounded px-4 py-3 hover:bg-red-600 transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round"
                        d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                <span>إعادة تشغيل النظام</span>
            </button>
        </div>
    </div>

    <div id="tab-content-maintenance" class="tab-content hidden">
        <div class="bg-gray-50 p-6 rounded-lg border border-gray-200 space-y-4">
            <div class="flex justify-between items-center border border-gray-300 rounded p-4">
                <div>
                    <h3 class="font-semibold text-lg">وضع الصيانة</h3>
                    <p class="text-gray-600">منع الوصول للمستخدمين مؤقتًا أثناء الصيانة</p>
                </div>
                <button class="bg-red-500 text-white rounded px-4 py-2 hover:bg-red-600 transition-colors">تفعيل وضع
                    الصيانة</button>
            </div>
            <div class="flex justify-between items-center border border-gray-300 rounded p-4">
                <div>
                    <h3 class="font-semibold text-lg">إحصائيات النظام</h3>
                    <p class="text-gray-600">عرض إحصائيات الأداء والاستخدام</p>
                </div>
                <div>
                    <p>المستخدمون النشطون: <strong>24</strong></p>
                    <p>استخدام الذاكرة: <strong>68%</strong></p>
                    <p>استخدام المعالج: <strong>45%</strong></p>
                </div>
            </div>
            <div class="border border-gray-300 rounded p-4 bg-yellow-100 text-yellow-800 font-semibold">
                آخر نسخة احتياطية
                <span class="block text-sm font-normal">2024-01-20 الساعة 03:00 صباحًا</span>
            </div>
        </div>
    </div>
</div>

<script>function initializeTabs() {
        const tabs = ['general', 'tools', 'maintenance'];

        function openTab(tabName) {
            tabs.forEach(tab => {
                const content = document.getElementById('tab-content-' + tab);
                const button = document.getElementById('tab-' + tab);
                if (content) content.classList.add('hidden');
                if (button) {
                    button.classList.remove('border-b-0', 'bg-white', 'text-gray-700', 'font-semibold');
                    button.classList.add('text-gray-500');
                }
            });
            const activeContent = document.getElementById('tab-content-' + tabName);
            const activeButton = document.getElementById('tab-' + tabName);
            if (activeContent) activeContent.classList.remove('hidden');
            if (activeButton) {
                activeButton.classList.add('border-b-0', 'bg-white', 'text-gray-700', 'font-semibold');
                activeButton.classList.remove('text-gray-500');
            }
        }

        tabs.forEach(tab => {
            const button = document.getElementById('tab-' + tab);
            if (button) {
                button.addEventListener('click', () => openTab(tab));
            }
        });

        // Initialize first tab as active
        openTab('general');
    }

    // Initialize tabs on DOMContentLoaded
    document.addEventListener('DOMContentLoaded', initializeTabs);
</script>
<link rel="stylesheet" href="/css/tabs.css" />